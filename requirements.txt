sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5) — Streamlit (app.py) and/or Gradio (app_gradio.py):
streamlit>=1.40.0
gradio>=6.9.0

# Hybrid search (stretch) — BM25 keyword retrieval fused with semantic search:
rank-bm25>=0.2.2

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
